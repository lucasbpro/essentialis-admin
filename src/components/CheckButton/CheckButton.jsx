import React from 'react';

const CheckButton = ({onClick}) => {

    return(
        <button onClick={onClick} > 
            <p> CHECAR LOJA </p>
        </button>
    )
}

export default CheckButton;